version: '3.8'

services:
  json-formatter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: json-formatter-app
    ports:
      - "8090:80"
    restart: unless-stopped
    labels:
      - "com.json-formatter.description=JSON Formatter & Validator Web Application"
